# WatermarkLab is designed for offline-first local operation.
# No cloud AI API key is required for normal runtime operation.
# Configure local model paths and application-specific development settings here
# only when required by the local development environment.
